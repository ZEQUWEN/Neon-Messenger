perl -0777 -pi -e 's/    var username by remember \{ mutableStateOf\(activeAccount.username\) \}/    var username by remember \{ mutableStateOf\(activeAccount.username\) \}\n    var usernameError by remember \{ mutableStateOf\<String\?\>\(null\) \}/g' app/src/main/java/com/example/ui/SettingsScreens.kt

perl -0777 -pi -e 's/                value = username,\n                onValueChange = \{ username = it \},\n                label = \{ Text\("\@username"\) \},\n                modifier = Modifier.fillMaxWidth\(\)/                value = username,\n                onValueChange = {\n                    username = it\n                    if (it.length < 5 && it.isNotEmpty()) {\n                        usernameError = "Username must be at least 5 characters"\n                    } else if (!it.matches(Regex("^[a-zA-Z0-9_]+\$")) && it.isNotEmpty()) {\n                        usernameError = "Invalid characters"\n                    } else if (it == "admin" || it == "system") {\n                        usernameError = "Username is already taken"\n                    } else {\n                        usernameError = null\n                    }\n                },\n                label = { Text("@username") },\n                modifier = Modifier.fillMaxWidth(),\n                isError = usernameError != null,\n                supportingText = { usernameError?.let { Text(it) } }/g' app/src/main/java/com/example/ui/SettingsScreens.kt

perl -0777 -pi -e 's/                    viewModel.updateProfile\(activeAccount.id, username, displayName, bio, profilePicUrl\)\n                    navController.popBackStack\(\)/                    if (usernameError == null) {\n                        viewModel.updateProfile\(activeAccount.id, username, displayName, bio, profilePicUrl\)\n                        navController.popBackStack\(\)\n                    }/g' app/src/main/java/com/example/ui/SettingsScreens.kt
